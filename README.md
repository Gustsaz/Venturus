# Venturus

/venturus
├── frontend/       ← projeto React(Node, npm, etc.)
│   └── src/
│       └── pages/
│       └── App.jsx
├── backend/        ← projeto PHP (com API)
│   └── public/
│   └── api/
│       └── login.php
│       └── cadastro.php
